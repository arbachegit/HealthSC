/** basePath do app — deve casar com next.config.js (§7 portabilidade). */
export const BASE_PATH = '/discoveryHealth'
/** Destino canônico de saída do deck (§3). */
export const EXIT_HREF = 'https://iconsai.ai/intelligenceHub'
